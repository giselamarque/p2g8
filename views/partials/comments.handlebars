<div class="body">
  {{#each this}}
  <section>
    <div class="meta">
      {{user.username}} on {{post.created_at}}
    </div>
    <div class="text">
      {{comment.comment_text}}
    </div>
  </section>
  {{/each}}
</div>